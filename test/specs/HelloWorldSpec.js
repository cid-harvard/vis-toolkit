describe("Hello world my version is", function() {
  it("says hello and the current version", function() {
    expect(vistk.version).toEqual("0.2");
  });
});

describe('JavaScript addition operator', function () {
    it('adds two numbers together', function () {
        expect(1 + 2).toEqual(3);
    });
});
