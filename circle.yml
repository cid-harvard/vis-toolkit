general:
  artifacts:
    - build/vistk.js
test:
  post:
    - grunt
deployment:
  release:
    tag: /v?[0-9]+(\.[0-9]+)*-(\w*)/
    owner: romsson
    commands:
      - echo "Deployment script goes here".
